/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://abhinavs-dev-canvas.netlify.app', // 🔑 change if you use custom domain later
  generateRobotsTxt: true, // also generate robots.txt for SEO
  sitemapSize: 7000,
};
